A program which can seemingly answer any question put to it.

The trick lies in an 'overlay mode' which is trigged with a special key. 
When this is activated, any text typed will not echo and instead a pre selected text is echoed out.
 
Toggle overlay mode with the ';' key. 
